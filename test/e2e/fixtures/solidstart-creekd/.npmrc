# See test/e2e/fixtures/nuxt-creekd/.npmrc — same rationale: vitest in the
# parent workspace pulls estree-walker@3.0.3, conflicts with the v2 that
# nitropack/Vinxi need at runtime. Hoist to isolate.
node-linker=hoisted
